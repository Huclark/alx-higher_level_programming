-- Script that creates a table called 'first_table' in a given MySQL database

-- Query to create the table if it doesn't exist
CREATE TABLE IF NOT EXISTS first_table (
	id INT,
	name VARCHAR(256)
);
